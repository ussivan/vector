#include <iostream>
#include "vector.h"

//int main() {
//    vector<int> c;
//    c.push_back(0);
//    c.push_back(1);
//    for (size_t i = 0; i != 20; ++i)
//        c.push_back(i);
//    for (size_t i = 0; i != 22; ++i)
//        std::cout << (int)i % 2 << " " << c[i] << std::endl;
//    return 0;
//}